{
  "name": "breakup-recovery-app",
  "scripts": {
    "dev": "concurrently \"cd backend && uvicorn main:app --reload --port 8000\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
